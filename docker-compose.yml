version: '3'

services:
  backend:
    build: ./backend
    command: ["bash", "docker-entrypoint.sh"]
    volumes:
      - ./backend:/opt/backend
      - ./data:/opt/data
    ports:
      - "8000:8000"
